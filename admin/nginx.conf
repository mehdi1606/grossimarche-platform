# Serves the admin SPA under /admin/ with client-side-routing fallback.
server {
  listen 80;
  server_name _;

  location = /admin { return 301 /admin/; }

  location /admin/ {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /admin/index.html;
  }
}
