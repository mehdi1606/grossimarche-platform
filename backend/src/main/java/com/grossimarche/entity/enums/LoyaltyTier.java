package com.grossimarche.entity.enums;

/**
 * Loyalty tier by lifetime points. Thresholds and multipliers are defined in
 * configuration (LoyaltyProperties, B7), not here — this enum is only the identity.
 */
public enum LoyaltyTier {
    BRONZE,
    ARGENT,
    OR
}
